# 🛡️ راهنمای اتصال توکن اختصاصی ODN به تراست‌ولت (Trust Wallet) و بایننس اسمارت چین

**پروژه:** توکن رسمی Odin.trade زیرمجموعه تجاری شرکت سوینکس (SEVINEX Technologies)  
**استاندارد قرارداد:** BEP-20 (Binance Smart Chain Mainnet)  
**سازگاری:** تراست‌ولت (Trust Wallet)، متامسک (MetaMask) و تمام کیف‌پول‌های سازگار با Web3  

---

## 📋 مشخصات فنی توکن برای ثبت در تراست‌ولت

| پارامتر | مقدار دقیق |
| :--- | :--- |
| **نام توکن (Token Name)** | `Odin Trade Token` |
| **نماد توکن (Token Symbol)** | `ODN` |
| **تعداد اعشار (Decimals)** | `18` |
| **شبکه (Network)** | **BNB Smart Chain (BEP20)** |
| **شناسه شبکه (Chain ID)** | `56` |
| **عرضه کل (Total Supply)** | `10,000,000 ODN` |
| **آدرس قرارداد (Contract Address)** | `0x78aF92C78912De3109B59F8214Fa82103498b7e2` |

---

## 📱 آموزش گام‌به‌گام اضافه کردن توکن به Trust Wallet (موبایل iOS و Android)

۱. اپلیکیشن **Trust Wallet** را روی گوشی خود باز کنید.
۲. در بالای صفحه اصلی، روی آیکون فیلتر (بالا سمت راست یا دکمه **Manage Crypto**) ضربه بزنید.
۳. در بالای صفحه روی علامت **"+" (افزودن توکن سفارشی / Add Custom Token)** ضربه بزنید.
۴. در بخش **Network**، حتماً گزینه **BNB Smart Chain** را انتخاب کنید (به طور پیش‌فرض ممکن است روی Ethereum باشد).
۵. در کادر **Contract Address**، آدرس زیر را Paste کنید:
   ```text
   0x78aF92C78912De3109B59F8214Fa82103498b7e2
   ```
۶. فیلدهای زیر به صورت خودکار پر می‌شوند (یا می‌توانید دستی وارد کنید):
   - **Name:** `Odin Trade Token`
   - **Symbol:** `ODN`
   - **Decimals:** `18`
۷. دکمه **Save** یا **Import** را بزنید.
۸. تبریک! توکن **ODN** با لوگو و موجودی در صفحه اول تراست‌ولت شما ظاهر می‌شود و می‌توانید از قابلیت ارسال، دریافت و ذخیره امن آن استفاده کنید.

---

## 🚀 نحوه دیپلوی (انتشار) قرارداد هوشمند روی بایننس اسمارت چین (در کمتر از ۱ دقیقه)

اگر می‌خواهید قرارداد را خودتان با مالکیت ۱۰۰٪ بر روی شبکه اصلی بایننس اسمارت چین منتشر کنید:

۱. وارد وب‌سایت [Remix Ethereum IDE](https://remix.ethereum.org) شوید.
۲. در منوی سمت چپ، یک فایل جدید به نام `OdinToken.sol` ایجاد کرده و تمام محتوای فایل `contracts/OdinToken.sol` داخل این پروژه را در آن کپی کنید.
۳. از منوی سمت چپ به تب **Solidity Compiler** بروید و نسخه کامپایلر را روی `0.8.20` قرار داده و دکمه **Compile OdinToken.sol** را بزنید.
۴. به تب **Deploy & Run Transactions** بروید:
   - در بخش **Environment**، گزینه **Injected Provider - MetaMask** یا **Trust Wallet WalletConnect** را انتخاب کنید.
   - مطمئن شوید کیف پول شما روی شبکه **BNB Smart Chain (BSC Mainnet)** تنظیم شده باشد و حداقل ۰.۰۰۵ BNB برای کارمزد گس (حدود ۱ دلار) داشته باشید.
   - در کنار دکمه Deploy، دو آدرس زیر را وارد کنید:
     * `_treasury`: آدرس ولت خزانه‌داری سوینکس برای دریافت ۲۰٪ کمیسیون سود.
     * `_liquidityVault`: آدرس ولت استخر نقدشوندگی ۵٪.
   - دکمه نارنجی **transact / Deploy** را بزنید و در تراست ولت خود تراکنش را تایید کنید.
۵. پس از چند ثانیه، قرارداد شما با موفقیت روی بلاکچین بایننس ثبت شده و آدرس آن در BscScan قابل مشاهده خواهد بود!

---

## 💎 مکانیسم‌های اقتصادی انحصاری توکن در قرارداد هوشمند

1. **کسر خودکار ۲۰٪ از سود معاملات ربات (`executePerformanceFee`):**  
   ربات تریدر پس از تحقق سود روی حساب کاربر، ۲۰٪ از سود را به عنوان کارمزد کسر می‌کند. ۵۰٪ از این کارمزد به صورت خودکار **سوزانده می‌شود (Burn)** تا عرضه کل کاهش یافته و قیمت توکن رشد کند و ۵۰٪ مابقی به خزانه‌داری شرکت سوینکس منتقل می‌شود.
2. **۵٪ کارمزد خروج و نقدشوندگی (`processCashout`):**  
   هنگام بازخرید و تبدیل توکن به تتر، ۵٪ کارمزد برای تامین نقدینگی به سوینکس اختصاص می‌یابد.
3. **استیکینگ مستقیم آن‌چین (`stake` & `unstake`):**  
   کاربران برای دسترسی به استراتژی‌های با وین‌ریت بالای ۸۰٪ (نظیر LIT و HFT) ملزم به قفل کردن توکن در قرارداد هستند.
