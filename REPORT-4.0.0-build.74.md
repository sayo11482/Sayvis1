# گزارش تک ۱۵ — SAYVIS 4.0.0: بازسازی شبکه، مرکز اکانت گوگل، QR و تم ارنا

## ✅ تحویل‌شده

**Release:** [v4.0.0-build.74](https://github.com/sayo11482/Sayvis1/releases/tag/v4.0.0-build.74)
**APK:** [SAYVIS-4.0.0-debug.apk](https://github.com/sayo11482/Sayvis1/releases/download/v4.0.0-build.74/SAYVIS-4.0.0-debug.apk) (51MB + [SHA256](https://github.com/sayo11482/Sayvis1/releases/download/v4.0.0-build.74/SAYVIS-4.0.0-debug.apk.sha256))
**CI:** completed / success — versionCode 15 / versionName 4.0.0، کامیت سبز `0ff213b` + فیکس تست `1a4f8e0`

## 🌐 ۱) شبکه از اول ساخته شد — `SayvisNet`

پیش از این، **۱۳ نقطهٔ مختلف** هر کدام OkHttp کلاینت خودشان را می‌ساختند. حالا **یک موتور مشترک** برای همه:

- استخر اتصال مشترک + User-Agent یکسان (`SAYVIS/4.0`) روی هر بایت خروجی
- **سرآیند هویت `X-Sayvis-Account`** خودکار روی تمام درخواست‌ها — اکانت شما واقعاً مرکز مصرف شبکه/هوش مصنوعی است
- backoff نمایی برای تلاش مجدد (۶۰۰→۱۲۰۰→۲۴۰۰→۴۸۰۰ میلی‌ثانیه، سقف ۸ ثانیه) با تست واحد
- مهاجرت کامل: AI providers (Gemini/OpenAI/Groq/xAI/OpenRouter)، بازارها، وب‌سرچ، اسکن گیت‌هاب، توکن گوگل، پراب اتصال، درگاه MT

## 🔐 ۲) ریشهٔ مشکل لاگین پیدا و حذف شد

**علت:** دکمهٔ ورود گوگل تا وقتی کاربر Client ID از کنسول گوگل نمی‌گذاشت، **غیرفعال** بود؛ و مسیر OAuth بدون ثبت SHA-1 در کنسول همیشه با خطا شکست می‌خورد.

**راه‌حل واقعی (بدون هیچ تنظیماتی):** «انتخاب‌گر سیستمی اکانت گوگل» (`AccountManager.newChooseAccountIntent`) — همان شیت یک‌لمسی همهٔ اپ‌های معروف:

1. **`GoogleSignInActivity` بازنویسی شد**: باز شدن = خودکار شیت انتخاب اکانت گوگل → یک لمس → متصل
2. بعد از انتخاب، خط لولهٔ خودکار قبلی ادامه می‌یابد: کلید Gemini از کلیپ‌بورد گرفته → تأیید زنده → هوش مصنوعی وصل (صفر تایپ)
3. **جایگزین‌ها:** ورود دستی با ایمیل (دستگاه بدون سرویس گوگل) و مسیر OAuth پیشرفته برای Gmail/Calendar/Drive (اختیاری، حفظ شد)
4. دکمهٔ ورود در تنظیمات دیگر قفل نیست؛ **«مرکز اتصال سایویس»** هم یک دکمهٔ اصلی در تنظیمات است

## 🏠 ۳) مرکز اتصال — صفحهٔ جدید `ConnectScreen`

- **اکانت گوگل = مرکز همهٔ ورودها و هوش مصنوعی**: کارت اکانت (نام، ایمیل، آواتار) + خروج کامل (پاک‌سازی توکن‌ها و هویت)
- وضعیت زندهٔ شبکه با چراغ رنگی و پیام صادقانه (آنلاین / گوگل مسدود-VPN لازم / آفلاین)
- در اولین اجرای بدون اکانت، خودکار باز می‌شود؛ «فعلاً بعداً» هم هست

## 📱 ۴) QR اتصال — اسکن کامپیوتر (دو جهت واقعی)

- **نمایش QR:** QR جفت‌سازی دستگاه (`sayvis://link?v=1&acct=…&dev=…&pin=…`) با ZXing رندر می‌شود؛ با اسکنر کامپیوتر/گوشی دیگر خوانده می‌شود و اکانت+دستگاه+PIN جفت‌سازی را نشان می‌دهد؛ دستگاه دوم سایویس با اسکنش همان اکانت را لینک می‌کند
- **اسکن QR از کامپیوتر:** دوربین (CameraX + ML Kit Barcode روی دستگاه) هر QR روی مانیتور کامپیوتر را می‌خواند:
  - QR جفت‌سازی سایویس → لینک اکانت + audit
  - کلید سرویس (Gemini `AIza…`/`AQ.…`، OpenAI `sk-`، Groq `gsk_`، xAI، OpenRouter) → ذخیرهٔ خودکار در همان سرویس
  - کانفیگ JSON `{"provider":…,"apiKey":…,"model":…}` → ثبت کامل
  - لینک/متن → نمایش
- **جایگزین چسباندن متن** برای بدون‌دوربین‌ها؛ همهٔ عملیات‌ها با audit event ثبت می‌شوند

## 🎨 ۵) تم ارنا — مشکی-طوسی حرفه‌ای

پالت بازطراحی شد (همهٔ صفحه‌ها یکجا): پس‌زمینهٔ نزدیک سیاه `#0A0B0E`، سطوح گرافیتی `#14171B/#1C2026`، حاشیه `#272C33`، اکسنت نقره‌ای-سفید `#E8EAED` به‌جای فیروزه‌ای؛ طلایی → شامپاینی-نقره‌ای محدود؛ سبز/قرمز/کهربایی فقط برای دادهٔ بازار و وضعیت. چهرهٔ ربات و آواتار شناور هم هم‌رنگ شدند.

## 🧪 تست و CI

- تست‌های جدید: round-trip کدک QR (با یونیکد/فاصله/اعتبار سنجی PIN)، classify هر ۵ نوع payload، کلیدهای ۵ سرویس، ایمیل/نام نمایشی هاب، backoff شبکه
- دوگانهٔ پایتونی: کدک، classifier، backoff، ایمیل — همه PASS قبل از push
- **۶ دور CI:** خطای AccountPicker (کلاس عمومی نیست) → امضای ۶ آرگومان → امضای ۷ آرگومانی + خروجی Intent (از مستندات رسمی) → تایپ platform `MutableList` → فیکس fixture تست ✅

## 🧾 معیارها

- CI سبز ✅ — run [35019698532](https://github.com/sayo11482/Sayvis1/actions/runs/35019698532)
- Release + APK ✅ — v4.0.0-build.74
- اینترنت فعال و ساده ✅ — یک لمس گوگل + پراب زنده + شبکهٔ یکپارچه
- مرکز اکانت گوگل ✅ — هویت + سرآیند `X-Sayvis-Account` + خروج مرکزی + QR
- تم ارنا مشکی-طوسی ✅
- گزارش فارسی ✅ — همین سند
