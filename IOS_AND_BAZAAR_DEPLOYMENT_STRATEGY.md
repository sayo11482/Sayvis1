# 📱 استراتژی و راهنمای جامع انتشار اپلیکیشن Odin.trade در کافه بازار و سیستم‌عامل iOS (آیفون)

**پروژه:** Odin.trade PRO v1.0.28 — زیرمجموعه تجاری شرکت سوینکس (SEVINEX Technologies)  
**هدف:** حضور موفق و جهانی در کافه بازار (بازار ایران) و گوشی‌های آیفون (iOS) بدون ریسک بلاک و تحریم  

---

## 🟢 بخش اول: راهنمای گام‌به‌گام انتشار در کافه بازار (Bazaar Android)

کافه بازار سخت‌گیرانه‌ترین مارکت اندرویدی ایران در زمینه مسائل مالی و دسترسی‌ها است. برای تضمین تایید ۱۰۰٪ برنامه در کافه بازار، استانداردهای زیر در پروژه رعایت شده‌اند:

### ۱. تنظیمات `AndroidManifest.xml`
- **حذف دسترسی‌های حساس:** دسترسی‌های غیرضروری نظیر دسترسی به پیامک، لیست مخاطبین یا موقعیت مکانی دقیق (GPS) که منجر به ریجکت شدن توسط کارشناسان بازار می‌شود حذف شده و فقط دسترسی‌های استاندارد زیر باقی مانده‌اند:
  * `android.permission.INTERNET` (جهت دریافت دیتای زنده بازار و قیمت‌ها)
  * `android.permission.ACCESS_NETWORK_STATE` (بررسی اتصال اینترنت)
  * `android.permission.VIBRATE` (فیدبک لمسی سیگنال‌ها)

### ۲. نحوه مدیریت پرداخت در بازار
- **سیاست کافه بازار:** بازار اجازه استفاده از درگاه‌های پرداخت خارج از درگاه خود بازار را در صورت فروش کالا یا خدمات دیجیتال مستقیم داخل ایران نمی‌دهد.
- **راهکار استاندارد تایید شده:**  
  برنامه در بازار به عنوان **«دستیار تحلیلی و ابزار مدیریت ریسک کوانت»** دسته‌بندی می‌شود. از آنجا که کلیه محاسبات و واریز/برداشت‌ها با تتر (USDT) و توکن غیرمتمرکز ODN انجام می‌گیرد، این بخش در یک تب تحت عنوان «حساب کاربری وب۳» قرار می‌گیرد و به عنوان ابزار فین‌تک بین‌المللی با بروکر ویتاورس تایید می‌شود.

### ۳. ساخت کلید امضای نهایی (Release Keystore)
برای خروجی نهایی بازار، دستور زیر یک امضای رسمی ۲۵ ساله تولید می‌کند:
```bash
keytool -genkey -v -keystore odin-release.jks -alias odin-key -keyalg RSA -keysize 2048 -validity 10000
```
سپس بیلد نهایی در گیت‌هاب اکشنز یا سیستم محلی به صورت فرمت AAB یا APK امضاشده خروجی گرفته و در پنل توسعه‌دهندگان بازار آپلود می‌شود.

---

## 🍎 بخش دوم: نقشه راه انتشار و فعال‌سازی در آیفون و آیپد (iOS)

توسعه اپلیکیشن‌های مالی، فارکس و کریپتو برای iOS همواره با دو چالش بزرگ مواجه است:
1. **قوانین سخت‌گیرانه اپ استور (Apple Guidelines 3.1.1):** اپل برای هرگونه پرداخت، ۳۰٪ سهم کارمزد برمی‌دارد و اتصال مستقیم به ولت‌های غیرمتمرکز رمزارزی را به راحتی نمی‌پذیرد.
2. **هزینه و تحریم اکانت اپل دولوپر (Apple Developer Program):** نیاز به اکانت سالانه ۹۹ دلاری با هویت و کارت خارجی.

### راهکار طلایی شماره ۱: نسخه فوق‌پیشرفته PWA (نصب بومی روی آیفون بدون نیاز به اپ‌استور)
بزرگ‌ترین صرافی‌ها و پلتفرم‌های ترید جهان (نظیر Uniswap و TradingView) از این راهکار استفاده می‌کنند:
1. وب‌داشبورد پروژه مجهز به فایل `manifest.json` و Service Worker اختصاصی iOS شده است.
2. کاربر آیفون آدرس داشبورد را در مرورگر **Safari** باز می‌کند.
3. دکمه **Share** (مربع با فلش رو به بالا) را در پایین صفحه لمس کرده و گزینه **«Add to Home Screen» (افزودن به صفحه اصلی)** را انتخاب می‌کند.
4. اپلیکیشن با آیکون طلایی Odin.trade و نام رسمی دقیقاً مانند یک اپلیکیشن نیتیو اپ استور روی صفحه آیفون قرار می‌گیرد و بدون باز شدن نوار آدرس مرورگر (حالت تمام صفحه Full Screen) اجرا می‌شود.
5. **ارتباط عمیق با Trust Wallet در آیفون:** با زدن دکمه اتصال کیف پول، تراست ولت نصب شده روی آیفون به صورت خودکار باز شده و تراکنش‌ها را در داخل شبکه بایننس اسمارت چین تایید می‌کند!

### راهکار شماره ۲: نسخه بومی Compose Multiplatform (iOS Framework)
چون کدهای پروژه در `odin-agent` بر اساس **Kotlin** نوشته شده‌اند، ساختار لایه منطق محاسباتی (`SymbolManager`, `QuantitativeModels`, `RiskManager`, `TokenEngine`) کاملاً مستقل از کدهای اندروید بوده و با پلتفرم KMP (Kotlin Multiplatform) سازگار است. بدین ترتیب می‌توان با اضافه کردن تارگت iOS در Gradle، خروجی Framework برای Xcode دریافت کرده و فایل `.ipa` بومی برای انتشار در استورهای ایرانی iOS (مانند سیبچه، اناردونی، آی‌اپس و سیب‌اپ) خروجی گرفت.

---

## 📊 چک‌لیست پتانسیل جهانی شدن پلتفرم (Global Scalability)

برای اینکه یک نرم‌افزار پتانسیل رقابت با پلتفرم‌های وال‌استریت و شرکت‌های کوانت جهانی را داشته باشد، موارد زیر رعایت شده‌اند:
- [x] **پشتیبانی کامل دوزبانه (فارسی و انگلیسی حرفه‌ای مالی)**
- [x] **محاسبه خودکار اسپرد، اسلیپیج و سواپ شبانه بروکر ویتاورس**
- [x] **پوشش ۹۰ نماد معتبر جهانی (ماژور، کراس، طلا، نفت، داوجونز، نزدک، کریپتو)**
- [x] **قرارداد هوشمند استاندارد BEP-20 روی زنجیره بایننس برای اتصال به Trust Wallet**
- [x] **مدل درآمدی پایدار (۲۰٪ کمیسیون از سود و ۵٪ کارمزد خروج برای شرکت سوینکس)**
- [x] **امنیت تراکنش‌ها با دفترکل ضد دستکاری SHA-256 و تایید دو مرحله‌ای (2FA)**
