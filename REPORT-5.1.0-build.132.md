# گزارش تک ۱۷ — SAYVIS 5.1.0: هویت اتمی طلایی (آیکون + هستهٔ تنفس‌دار + مارکت لایو)

## ✅ تحویل‌شده

**Release:** [v5.1.0-build.132](https://github.com/sayo11482/Sayvis1/releases/tag/v5.1.0-build.132)
**APK:** [SAYVIS-5.1.0-debug.apk](https://github.com/sayo11482/Sayvis1/releases/download/v5.1.0-build.132/SAYVIS-5.1.0-debug.apk) + [SHA256](https://github.com/sayo11482/Sayvis1/releases/download/v5.1.0-build.132/SAYVIS-5.1.0-debug.apk.sha256)
**CI:** completed / success — versionCode 18 / versionName 5.1.0، کامیت سبز `c702ceb`

**همهٔ تنظیمات قبلی دست‌نخورده ماند — فقط محیط گرافیکی عوض شد.**

## 🅰️ ۱) آیکون اتمی «S» — الگوبرداری مستقیم از نمونهٔ شما

با هوش مصنوعی تصویری، آیکونی دقیقاً در سبک نمونهٔ شما ساخته شد: **«S» طلایی براق در مرکز + سه مدار بیضی الکترونی درخشان + الکترون‌های طلایی + ردپای مدارهای IC در پس‌زمینهٔ مشکی** — و در **همهٔ چگالی‌ها** نصب شد:

- legacy: webp در ۴۸/۷۲/۹۶/۱۴۴/۱۹۲ پیکسل (ic_launcher + ic_launcher_round با ماسک دایره‌ای)
- adaptive: لایهٔ foreground در بوم ۴۳۲×۴۳۲ با امن‌زون درست + پس‌زمینهٔ گرادیان مشکی (`#0A0B0E→#1A1D22`)

## 🫀 ۲) هستهٔ گردان «تنفس‌دار» — هم‌گام با تفکر هوش مصنوعی

`SayvisAvatar` کاملاً بازنویسی شد — راکتور اتمی طلایی که مثل تنفس حرکت می‌کند و **با فعالیت هوش مصنوعی نفس می‌کشد**:

| حالت | رفتار هسته |
|---|---|
| آیدی resting | چرخش آرام + نفس ۲٫۴ثانیه‌ای (دم/بازدم نور) |
| **تفکر** | چرخش ۳برابری سریع + درخشش بیشتر (سوختن راکتور) |
| صحبت | پالس هسته با هر سیکل (نفسِ صدا) |
| فرصت آگاه | دنباله‌دار طلایی روی مدارها می‌دود |
| قفل اضطراری | مدارها قرمز و تقریباً منجمد |

سه مدار بیضی با سرعت‌های متفاوت (یکی معکوس)، ۶ الکترون با درخشش رهبر، هالهٔ تنفسی، و **میکروفون** روی شعاع هسته اثر می‌گذارد. این هسته در خانه، چت و همهٔ صفحه‌ها یکجا عوض شد.

## 🥇 ۳) پالت طلایی روی مشکی (مطابق نمونه)

اکسنت تعاملی سراسری → **طلایی `#D4AF37`** (دکمه‌ها، سوییچ‌ها، هایلایت‌ها)، طلایی روشن `#F3CA68` برای اکشن‌های اصلی، کانتینرهای گرم `#3A2F0B`؛ سطوح همان مشکی-گرافیتی قبلی. چهرهٔ ربات، آواتار شناور و همهٔ صفحه‌ها هم‌خانواده شدند.

## 📈 ۴) نمادهای واقعی — نوار «مارکت لایو» در خانه

کارت جدید زیر هیرو: **قیمت‌های زندهٔ واقعی** (طلا، یورو/دلار، دلار/ریال، تتر/تومان) از همان دیتاسورس‌های زندهٔ بازار + درصد تغییر ۲۴ساعته سبز/قرمز + **اسپارک‌لاین چارت زنده** (Canvas) از آخرین ۶۰ نقطهٔ سری واقعی — همان روح «چارت لایو مارکت فورکس» در نمونهٔ شما، مستقیم روی خانه.

+ خط «سایو · پلتفرم هوش مصنوعی شخصی» بالای تایتر هیرو (مطابق چیدمان نمونه).

## 🧪 CI

۲ دور: `SayvisGoldLight` alias جامانده + nullable-safe بودن سری و ریاضیات Float اسپارک‌لاین → ✅ سبز. هیچ تنظیمات/منطق داده‌ای تغییر نکرد؛ فقط رندر.

## 🧾 معیارها

- CI سبز ✅ — run [35469363341](https://github.com/sayo11482/Sayvis1/actions/runs/35469363341)
- Release + APK ✅ — v5.1.0-build.132
- آیکون اتمی همه‌ی چگالی‌ها ✅ | هستهٔ تنفس‌دار ✅ | پالت طلایی ✅ | نمادهای لایو ✅ | تنظیمات حفظ ✅
- گزارش فارسی ✅ — همین سند
