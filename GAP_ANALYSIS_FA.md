# 📋 گزارش تحلیل گپ — SAYVIS
### مقایسه‌ی سند اصلی (README / ARCHITECTURE / API / AWARE / UIC / SECURITY / THREAT_MODEL) با کد فعلی

**تاریخ بررسی:** 1405/06/22 (2026-09-13) — شاخه: `arena/01a099b3-sayvis1`
**حجم کد:** ~۶,۸۰۰ خط Kotlin در ۳۷ فایل + لایه‌ی داده Room با ۶ جدول

---

## ۱) خلاصه‌ی وضعیت ماژول‌ها

| # | ماژول (طبق سند) | وضعیت | توضیح |
|---|---|---|---|
| 1 | UIC — مدل شناختی کاربر | ✅ کامل | ۵ وضعیت معرفت‌شناختی، اطمینان ۰–۱، provenance، حریم خصوصی، UI کامل (مشاهده/تأیید/لغو/حذف/افزودن) |
| 2 | موتور AWARE | ✅ کامل | تشخیص الگو، فرصت‌های پیشنهادی، ریسک‌اسکور، مجوزها، اسکن دستی |
| 3 | AI Orchestrator | ✅ کامل | مسیریابی Gemini ↔ لوکال، حالت آفلاین، قفل اضطراری |
| 4 | Zero-Trust Permission Engine | ✅ کامل | دروازه ریسک، رضایت صریح مالک، قفل اضطراری |
| 5 | موتور مأموریت‌ها | ✅ کامل | مأموریت/وظیفه، درصد پیشرفت خودکار، ۶ وضعیت، افزودن مأموریت |
| 6 | آواتار Sovereign | ✅ کامل | Canvas زنده با ۶ حالت شناختی/امنیتی |
| 7 | دوزبانه RTL/LTR | ✅ کامل | سوییچ فارسی/انگلیسی + LayoutDirection |
| 8 | **حسابرسی زنجیره‌ای SHA-256** | ❌ **جاافتاده** | جدول audit فقط رخداد خام ذخیره می‌کند — بدون `previous_event_hash` و `event_hash` |
| 9 | سیستم حافظه (Memory) | ⚠️ ناقص | جدول + DAO موجود ولی بدون UI و بدون اتصال به AI Orchestrator |
| 10 | LIT (تحلیل معاملات) | ⚠️ ناقص | فقط مدل‌ها + دروازه Paper-Trading + ۲ سیگنال دمو ثابت — بدون موتور تحلیل و دیتافید |
| 11 | شبیه‌ساز زندگی | ⚠️ ناقص | فقط ۲ سناریوی دموی ثابت — بدون موتور احتمالاتی |
| 12 | امنیت دستگاه‌های همراه | ⚠️ ناقص | لیست/اعتماد/لغو دستگاه موجود ولی **بدون جفت‌سازی رمزنگاری واقعی** (اثر انگشت کلید، دمو استاتیک) |
| 13 | تله‌متری واقعی AWARE | ⚠️ ناقص | باتری/شبکه/حرارت به‌جای سنسور واقعی، مقادیر فرضی است (BatteryManager/ConnectivityManager متصل نشده) |
| 14 | اعتبارسنجی Prompt Injection | ⚠️ ناقص | ورودی کاربر بدون پاک‌سازی مستقیم به پرامپت Gemini تزریق می‌شود (ریسک با معماری ProposedAction کاهش یافته) |
| 15 | **REST API Gateway (`/api/v1/...`)** | ❌ **جاافتاده** | هیچ کد سروری در ریپو نیست (auth / ai / sync / audit) |
| 16 | **همگام‌سازی چنددستگاهی** | ❌ **جاافتاده** | event-sourced replication پیاده‌سازی نشده |
| 17 | **Provایderهای Groq / OpenRouter** | ❌ **جاافتاده** | در enum تعریف شده‌اند (`GROQ_ROUTER`, `OPEN_ROUTER`) ولی کلاس Provider برایشان وجود ندارد |
| 18 | احراز هویت بیومتریک | ❌ جاافتاده | سند و seed داده‌ها به بیومتریک/passkey اشاره دارند؛ BiometricPrompt استفاده نشده (رضایت با دیالوگ ساده است) |
| 19 | اسناد ارجاعی API.md | ⚠️ ناقص | فایل‌های AI_ORCHESTRATION.md، MEMORY.md، LIT.md، TESTING.md و… که API.md به آن‌ها ارجاع می‌دهد در ریپو وجود ندارند (احتمالاً بخشی از پرامپت اصلی AI Studio بوده‌اند) |

---

## ۲) گپ‌های بحرانی (پیشنهاد اصلاح فوری)

### 🔴 گپ ۱ — زنجیره‌ی حسابرسی ضدتقلب (TM-03)
سند SECURITY.md قول می‌دهد هر رخداد شامل `payload_hash`، `previous_event_hash` و `event_hash = SHA256(...)` باشد و THREAT_MODEL.md می‌گوید شکست زنجیره = فعال‌سازی خودکار قفل اضطراری.
**واقعیت کد:** `AuditEventEntity` فقط ۹ فیلد ساده دارد؛ هیچ هش محاسبه یا ذخیره نمی‌شود. تست `zeroTrust_tamperEvidentSha256_producesDeterministicChain` فقط مفهوم را به‌صورت مستقل تست می‌کند، نه کد اپ را.
**اثر:** ادعای «Tamper-Evident» فعلاً صرفاً مستنداتی است.

### 🔴 گپ ۲ — سرویس Backend /api/v1
سند API.md چهار گروه endpoint (auth چندعاملی، دروازه AI، sync، audit) را توصیف می‌کند. در ریپو هیچ سروری (Ktor/Spring/Node) وجود ندارد. اپ کاملاً offline-first است؛ این بخش یا باید حذف از مستندات شود یا در نسخه‌های بعدی ساخته شود.

### 🔴 گپ ۳ — Providerهای Groq و OpenRouter
`ProviderType` آن‌ها را معرفی می‌کند اما `AIOrchestrator` فقط `GeminiProvider` و `LocalCognitiveProvider` را می‌شناسد؛ انتخاب این دو در UI عملاً بی‌اثر/غیرممکن است.

---

## ۳) ناهماهنگی‌های کوچک (Drift بین سند و کد)

| مورد | سند | کد |
|---|---|---|
| سطح حریم خصوصی UIC | `STANDARD / SENSITIVE / CONFIDENTIAL` | `STANDARD / PROTECTED / CONFIDENTIAL` |
| Provenance | enum مشخص (`USER_EXPLICIT`, `SYSTEM_OBSERVED`, …) | رشته آزاد |
| وضعیت مأموریت | `PLANNED, ACTIVE, COMPLETED, PAUSED` | `PLANNED, ACTIVE, AT_RISK, BLOCKED, COMPLETED, CANCELLED` (کد کامل‌تر است ✔) |
| نمایش نام Gemini | — | `Google Gemini 3.5 Flash / Pro` (مدل API: `gemini-3.5-flash` — معتبر و پایدار ✔) |

---

## ۴) چیزهایی که سند هست و درست پیاده شده ✅

- استقلال کامل خروجی AI از اجرا (`ProposedAction` هیچ‌گاه مستقیم اجرا نمی‌شود — همه‌چیز از دروازه Zero-Trust می‌گذرد)
- قفل اضطراری: مسدودسازی موتور AWARE، Orchestrator و اجرای فرصت‌ها + ثبت رخداد امنیتی
- LIT: معامله واقعی در سطح معماری مسدود (`liveTradingBlocked = true` پیش‌فرض، `killSwitchEngaged = true`)
- حریم خصوصی: `zero-telemetry` — هیچ داده‌ای از دستگاه خارج نمی‌شود (فقط فراخوانی مستقیم Gemini API در صورت تنظیم کلید)
- آفلاین-فرست: هسته محلی `LocalCognitiveProvider` بدون اینترنت پاسخ می‌دهد
- تست‌های JVM: ۴ تست برای UIC/AWARE/SHA-256/برچسب ریسک + تست اسکرین‌شات Roborazzi

---

## ۵) جمع‌بندی و پیشنهاد اولویت‌بندی

**تطابق کلی کد با سند: حدود ۷۵–۸۰٪** — هسته‌ی معماری (UIC، AWARE، Zero-Trust، مأموریت‌ها، آواتار) با وفاداری بالا پیاده شده؛ موارد جاافتاده عمدتاً در لایه‌های «اعتماد سخت‌افزاری» (زنجیره هش، بیومتریک، جفت‌سازی رمزنگاری) و «زیرساخت چنددستگاهی/سروری» متمرکزند.

**اولویت پیشنهادی اصلاحات:**
1. پیاده‌سازی زنجیره SHA-256 در `AuditEventEntity` + دیتابیس (نسخه ۲ اسکیم Room)
2. اتصال تله‌متری واقعی (BatteryManager / ConnectivityManager) به AWARE
3. UI مدیریت حافظه + تزریق حافظه در پرامپت AI
4. حذف یا پیاده‌سازی Providerهای Groq/OpenRouter
5. BiometricPrompt برای رضایت اقدامات CRITICAL

---

## ۶) به‌روزرسانی v1.2 — گپ‌های بسته‌شده (سپتامبر ۲۰۲۶)

| گپ قبلی | وضعیت در v1.2 |
|---|---|
| Providerهای Groq / OpenRouter | ✅ بسته شد — `OpenAiCompatibleProvider` + مسیریابی چندمدلی با fallback خودکار |
| کلید API فقط زمان بیلد | ✅ بسته شد — صفحه «تنظیمات» با کلید runtime (SharedPreferences محلی) + دکمه تست اتصال |
| تله‌متری فرضی AWARE | ✅ بسته شد — باتری/شارژ/شبکه/حافظه/RAM واقعی از BatteryManager/ConnectivityManager/StatFs |
| سیستم حافظه بدون اتصال | ✅ بسته شد — هر مکالمه به `memory_items` ذخیره و ۱۰ حافظه‌ی آخر به پرامپت تزریق می‌شود + تب «حافظه» |
| تحلیل فایل | ✅ جدید — دکمه 📎 در چت (SAF)؛ فایل‌های متنی خوانده و تحلیل می‌شوند |
| تغییر ظاهر توسط AI | ✅ جدید — پروتکل `sayvis-ui` : مدل می‌تواند رنگ اکسنت و زبان اپ را خودش تغییر دهد |

گپ‌های هنوز باز: زنجیره SHA-256، سرور `/api/v1`، بیومتریک، جفت‌سازی رمزنگاری دستگاه‌ها.
