# گزارش هات‌فیکس — SAYVIS 5.0.1: رفع خطای «اتصال ناموفق» جمینای

## ✅ تحویل‌شده

**Release:** [v5.0.1-build.129](https://github.com/sayo11482/Sayvis1/releases/tag/v5.0.1-build.129)
**APK:** [SAYVIS-5.0.1-debug.apk](https://github.com/sayo11482/Sayvis1/releases/download/v5.0.1-build.129/SAYVIS-5.0.1-debug.apk) + [SHA256](https://github.com/sayo11482/Sayvis1/releases/download/v5.0.1-build.129/SAYVIS-5.0.1-debug.apk.sha256)
**CI:** completed / success — versionCode 17 / versionName 5.0.1، کامیت سبز `f71f995`

## 🧯 ریشهٔ خطا (از اسکرین‌شات شما)

گوگل در پیام خطا گفت: **«model models/gemini-2.5-flash is no longer available to new users»** — یعنی مدل پیش‌فرض جمینایِ برنامه برای کاربران جدید **بازنشسته (retire)** شده و باید از `gemini-3.6-flash` استفاده شود. برنامه تا دیروز: کلید را می‌فرستاد، ۴۰۴ می‌گرفت و فقط «اتصال ناموفق» نشان می‌داد.

## 🔧 فیکس (چهار لایه، تا دیگر هیچ‌وقت با بازنشستگی مدل خاموش نشوید)

1. **زنجیرهٔ fallback خودکار مدل** در `GeminiProvider`: روی خطای «مدل بازنشسته شده» (۴۰۴ / no longer available / not found) به‌ترتیب `gemini-3.6-flash` → `gemini-flash-latest` → `gemini-2.0-flash` امتحان می‌شود و اولین پاسخ زنده برمی‌گردد؛ خطاهای دیگر (مثل ۴۰۳ تحریم جغرافیایی یا کلید بد) بلافاصله با همان پیام صادقانه نمایش داده می‌شوند
2. **پیش‌فرض‌ها به `gemini-3.6-flash`** منتقل شدند (تنظیمات + Store + راهنمای UI: «gemini-3.6-flash / gemini-flash-latest»)
3. **مهاجرت خودکار تنظیمات قدیمی:** نصب‌های فعلی که `gemini-2.5-flash` ذخیره کرده‌اند، در اولین اجرا به `gemini-3.6-flash` مهاجرت می‌یابند — حتی بدون باز کردن تنظیمات
4. **تشخیص خطای بازنشستگی** به‌صورت تابع خالص پیاده شد (پوشش «HTTP 404 + models/»، «no longer available»، «"code":404» و…) با تست واحد

نتیجه: دکمهٔ «آزمودن اتصال» و خودِ دستیار بعد از این آپدیت بدون هیچ دست‌کاری از سمت شما سبز می‌شوند.

## 🧪 تست و CI

- ۳ تست واحد جدید: زنجیرهٔ fallback (ترتیب/بدون تکرار)، تشخیص خطای بازنشسته vs خطای منطقه/کلید/تایم‌اوت، و ممنوع‌بودن مدل 2.5 به‌عنوان پیش‌فرض
- ۳ دور CI: باگ رشتهٔ Kotlin در شرط JSON (`"code":404` — با ثابت QUOTE حل شد) → شکست عجیب تست زنجیره → **بازنویسی دفاعی حلقهٔ صریح + پیام تشخیصی در خود تست** (اگر شکست می‌خورد، محتوای واقعی زنجیره در لاگ CI دیده می‌شد) → ✅ سبز
- تداخل تاریخچه با نشست موازی دیگر (ریباس تک‌کامیت — نسخهٔ نهایی من حفظ شد)

## 🧾 معیارها

- CI سبز ✅ — run [35468428509](https://github.com/sayo11482/Sayvis1/actions/runs/35468428509)
- Release + APK ✅ — v5.0.1-build.129
- گزارش فارسی ✅ — همین سند

> نصب کنید و «آزمودن اتصال» را بزنید — باید «🟢 اتصال برقرار است» با مدل `gemini-3.6-flash` ببینید. اگر خطای ۴۰۳/منطقه دیدید، آن دیگر مشکل مدل نیست: VPN لازم است (پیام داخل خود برنامه هم همین را می‌گوید).
