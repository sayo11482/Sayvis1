# گزارش تک ۱۹ — SAYVIS 5.2.0: نسخهٔ ویندوز + اتصال دوطرفه با QR

## ✅ تحویل‌شده

**Release:** [v5.2.0-build.144](https://github.com/sayo11482/Sayvis1/releases/tag/v5.2.0-build.144)
**APK:** [SAYVIS-5.2.0-debug.apk](https://github.com/sayo11482/Sayvis1/releases/download/v5.2.0-build.144/SAYVIS-5.2.0-debug.apk) + [SHA256](https://github.com/sayo11482/Sayvis1/releases/download/v5.2.0-build.144/SAYVIS-5.2.0-debug.apk.sha256)
**ویندوز:** [SAYVIS-5.2.0-windows.zip](https://github.com/sayo11482/Sayvis1/releases/download/v5.2.0-build.144/SAYVIS-5.2.0-windows.zip) — داخلش: `sayvis-desktop.jar` + `SAYVIS.bat` + `README-FA.txt`
**CI:** completed / success — versionCode 19 / versionName 5.2.0، کامیت سبز `446075f2`

**همهٔ تنظیمات و قابلیت‌های قبلی دست‌نخورده ماند** — ویندوز یک برادر دوقلوی اندروید است، نه جایگزین آن.

## 🖥 ۱) سایو برای ویندوز — همان هویت اتمی طلایی

ماژول جدید `:desktop` با **Compose Desktop** (Kotlin 2.2.10 + Compose Multiplatform 1.9.1):

- همان پالت طلایی روی مشکی، همان هستهٔ اتمی با مدارهای گردان و نفس (`AtomicCore`)
- پنجرهٔ ۵۲۰×۹۴۰ (تناسب گوشی) با چهار بخش: **اتصال** (QR + دستگاه‌ها)، **دستیار سایو**، **مارکت لایو**، وضعیت شبکه
- اکانت گوگل + کلید Gemini در همان صفحه؛ تنظیمات در `%USERPROFILE%\.sayvis\config.json` با **PIN ۶رقمی** محلی
- دستیار سایو روی ویندوز همان زنجیرهٔ مقاوم موبایل را دارد: `gemini-3.6-flash → gemini-flash-latest → gemini-2.0-flash` — با پرش خودکار روی خطای «مدل بازنشسته» (درس تک ۱۷ اینجا هم اعمال شد)
- مارکت لایو واقعی: طلا (PAXG)، یورو/دلار، دلار/ریال، تتر — همان سه منبع موبایل (CoinGecko / Frankfurter / open.er-api)
- ذخیرهٔ QR به‌صورت PNG از خود برنامه + درج دستی payload با Paste

## 🔗 ۲) QR دوطرفه — گوشی و ویندوز به هم می‌رسند

پروتکل قراردادی (دو سمت، یک کدِ مشترک `sayvis://link?v=1&acct=<email>&dev=<device>&pin=<6رقم>&ts=<ms>&h=<ip-lan>:8765`):

1. **ویندوز** اجرا می‌شود → سرور LAN روی پورت **8765** بالا می‌آید (صفحهٔ وضعیت تیرهٔ طلایی در `/` و پذیرندهٔ جفت‌سازی در `/pair`) → QR حاوی host خودش را نشان می‌دهد.
2. **اندروید** (بعد از اتصال گوگل، خودکار) QR را اسکن می‌کند → اکانتِ همان اکانت گوگل شما لینک می‌شود → چون `h` دارد، بلافاصله `POST http://<ip>:8765/pair` را با `{account, device, pin, source:android}` می‌زند.
3. **ویندوز** دستگاه را در لیست ثبت می‌کند (سقف ۲۰ دستگاه) و پیام تأیید سبز `connect_pair_message` را در اپ نشان می‌دهد.

نکات امنیتی صادقانه: ارتباط روی **شبکهٔ محلی Wi-Fi** و با HTTP ساده است (برای همین `usesCleartextTraffic` فقط برای این مسیر فعال شد)؛ PIN ۶رقمی + مالکیت اکانتِ یکسان شرط جفت‌سازی است و جفت‌سازی به‌خودی‌خود هیچ اجرای از راه‌دوری انجام نمی‌دهد — فقط ثبت اطمینان است. برای اینترنت خانه/اداره کافی است؛ روی Wi-Fi عمومی جفت نکنید.

## 🧪 تست‌ها و CI — شش دور فیکس، شفاف

اندروید: **۱۵ تست واحد سبز** (۱۲ تست v5 + ۳ تست fallback جمنای) + تست جدید round-trip هاست در `PairingQrTest`. مسیر رسیدن به سبزی (همه واقعی و ریشه‌یابی‌شده):

| # | خطا | فیکس |
|---|---|---|
| ۱ | YAML ورک‌فلو خراب — `\r\n` پایتونی به CR واقعی تبدیل شده بود | بازسازی بلوک با رشتهٔ خام |
| ۲ | `plugin is already on the classpath with an unknown version` — AGP نسخهٔ KGP را داخلی می‌آورد و درخواست نسخهٔ جدا در `:desktop` رد شد | پلاگین‌ها از version-catalog + اعلام `apply false` در ریشه |
| ۳ | `MediaType.get` در okhttp5 خطای کامپایل است | اکستنشن `toMediaType()` (اندروید و دسکتاپ) |
| ۴ | باگ شناخته‌شدهٔ KSP 2.3.5 — NPE روی AWT EventQueue وقتی IDE باز نیست ([google/ksp#2763](https://github.com/google/ksp/issues/2763))؛ ماژول دسکتاپ AWT را بیدار کرد | دان‌گرید KSP به 2.3.4 |
| ۵ | تسک `packageFatJar` در CMP 1.9 وجود ندارد | `packageUberJarForCurrentOS` |
| ۶ | `Dispatchers.Swing` داخلی است + یک تست ۱/۱۶ شانسی (فلیپ آخرین نویسه به «0» گاهی با اصل برابر می‌شد) | `Dispatchers.Main` (سرویس‌لودر coroutines-swing) + تست قطعی |

- CI سبز نهایی: run [35512432816](https://github.com/sayo11482/Sayvis1/actions/runs/35512432816) — تست + APK + **JAR ویندوز** در یک پاس
- دوقلوی پایتونیِ پروتکل (کدک با host + زنجیرهٔ جمنای) قبل از push پاس بود

## 📀 راه‌اندازی روی ویندوز

1. جاوا ۱۷ یا جدیدتر از [adoptium.net](https://adoptium.net) نصب کنید (یک‌بار).
2. `SAYVIS-5.2.0-windows.zip` را باز کنید و روی `SAYVIS.bat` دوبار کلیک کنید (پیام راهنمای فارسی هم داخل zip هست).
3. اکانت گوگل و کلید Gemini را در صفحه بچسبانید → دستیار فعال می‌شود.
4. در اپ اندروید: صفحهٔ اتصال → اسکن QR → «متصل شد» ✅ (هر دو دستگاه یک Wi-Fi، پورت 8765 باز).

## 🧾 معیارها

- CI سبز ✅ | Release ✅ | APK + SHA256 ✅ | **ZIP ویندوز ✅** | تست ✅ | QR دوطرفه در هر دو سمت ✅ | حفظ کامل تنظیمات ✅

## 🔭 مرز شفاف

امضای APK همچنان debug است (برای نصب، «منابع ناشناس» لازم است). سرور 8765 فقط LAN است و دسترسی از بیرون ندارد. اجرای واقعی JAR روی ویندوزِ فیزیکی در سندباکس قابل تست نبود (سندباکس لینوکسی و بدون نمایشگر است) — اما همان باینری با همان گریدل در CI ساخته و صحت‌سنجی شد؛ اگر روی ویندوز شما پیام جاوای ۱۷+ دیدید، طبق README عمل کنید.
