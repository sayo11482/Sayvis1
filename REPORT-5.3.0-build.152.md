# گزارش تک ۲۰ — SAYVIS 5.3.0: اپِ زنده، مرکز اتصال، ایجنت راهکار و ادغام‌گر گیت‌هاب

## ✅ تحویل‌شده

**Release:** [v5.3.0-build.152](https://github.com/sayo11482/Sayvis1/releases/tag/v5.3.0-build.152)
**APK:** [SAYVIS-5.3.0-debug.apk](https://github.com/sayo11482/Sayvis1/releases/download/v5.3.0-build.152/SAYVIS-5.3.0-debug.apk) + [SHA256](https://github.com/sayo11482/Sayvis1/releases/download/v5.3.0-build.152/SAYVIS-5.3.0-debug.apk.sha256)
**ویندوز:** [SAYVIS-5.3.0-windows.zip](https://github.com/sayo11482/Sayvis1/releases/download/v5.3.0-build.152/SAYVIS-5.3.0-windows.zip)
**CI:** completed / success — versionCode 20 / versionName 5.3.0، کامیت سبز `cf84265`
**تنظیمات و قابلیت‌های قبلی: همه دست‌نخورده** — فقط لایه‌های جدید روی همان بنا.

## 🟢 ۱+۳) مرکز اتصال: دکمهٔ آنلاین/آفلاین + سرعت زنده + قطعِ کامل

کارت «مرکز اتصال» بالای خانه نشست (`net_switch` / `net_speed` / `status_code_chip`):

- **دکمهٔ قرصی سبز/قرمز** — فشردنش کل اپ را قطع/وصل می‌کند. قطعِ واقعی است: یک گیت داخل خود OkHttp مشترک (`SayvisNet`) نصب شد (`LinkCenter.gateInterceptor`)؛ با آفلاین، **هر درخواستی قبل از باز شدن سوکت** با `OfflineException` رد می‌شود — هوش مصنوعی، مارکت، جست‌وجو، گیت‌هاب، گوگل، همه. حتی سنجش سرعت هم خاموش می‌شود (ترافیک پنهان وجود ندارد).
- **سرعت زندهٔ اینترنت** — دانلود واقعی از لینک Cloudflare و محاسبهٔ Mbps (فرمول خالص و تست‌شده)؛ خودکار هر ۴۵ ثانیه + با لمس. عدد فارسی با «مگابیت/ث».
- مرز شفاف: قطع «کل دستگاه» نیاز به اپ VPN و رضایت سیستم دارد؛ این قطع، **کل ترافیک داخل سایویس** است — دقیقاً همان چیزی که اپ کنترلش را دارد.

## 🧠 ۱) حافظه‌سپاری API — «یک‌بار متصل شد، برای همیشه»

اولین پاس موفق ابری (Gemini/…) مهر `aiLinkedOnce + aiLinkedAt + aiLinkedModel` را در تنظیمات می‌زند (کد ۰۴ «به حافظه سپرده شد»). از اجرای بعدی، سایویس **بدون پرسش دوباره** همان مغز را وصل می‌کند و فقط در پس‌زمینه ساکت سلامت‌سنجی می‌شود. اکانت گوگل هم از قبل با AccountManager دائمی است.

## 🔁 ۲) سامانهٔ اتصال پایدار + ⏱ ۱۰) اپِ زنده

`LinkCenter` یک **قلب تپندهٔ ۵ ثانیه‌ای** دارد: هر ۶ ضربان (≈۳۰ث) مارکت ساکت و بی‌صدا رفرش می‌شود، هر ۹ ضربان سرعت سنجیده می‌شود، و صفحه‌ها با `liveTick` همیشه «X ثانیه پیش» را نشان می‌دهند (`market_freshness`). پروب سه‌حالتهٔ قبلی (آنلاین/بدون‌گوگل/آفلاین) صدا زده می‌شود و کد وضعیت می‌دهد. دیتا دیگر هیچ‌وقت کهنه نمی‌ماند.

## 🎖 ۸) کدهای وضعیت و خطا — «۰۱ = در حال فکر کردن»

رجیستری کد ۰۰ تا ۱۰ با برچسب فارسی/انگلیسی و تاریخچهٔ ۳۰ آخری: `۰۰ بررسی اتصال، ۰۱ در حال فکر کردن، ۰۲ آفلاین(دکمه)، ۰۳ اینترنت قطع، ۰۴ هوش مصنوعی متصل شد، ۰۵ تعویض مدل، ۰۶ اینترنت هست/گوگل مسدود، ۰۷ مارکت به‌روز شد، ۰۸ ایجنت در حال کار، ۰۹ پروندهٔ شناختی به‌روز شد، ۱۰ اسکن گیت‌هاب`. چیپ طلایی کد فعلی روی خانه + داخل حبابهٔ تفکر.

## 🕵️ ۴) ایجنت راهکاریاب در مأموریت‌ها — فقط صوتی نیست

کارت جدید در «مأموریت‌ها» (`mission_agent_input/run/result`): هدف می‌دهی → ایجنت **واقعاً وب را جست‌وجو می‌کند، متن منابع را می‌خواند** (همان حلقهٔ چندمرحله‌ای AgentService)، با مغز AI سنتز می‌کند، از پاسخ **گام‌های اجرایی** را بیرون می‌کشد (پلنر خالص و تست‌شده) و **مأموریت واقعی با زیرکارهای تیک‌خور** در همان لیست مأموریت‌ها می‌سازد + هدف را در پروندهٔ شناختی ثبت می‌کند (کد ۰۸).

## 📂 ۵) پروندهٔ شناختیِ خودبه‌روز

موتور `CognitiveIngest` (خالص، ۱۴ تست) فعالیت واقعی شما را به ویژگی‌های UIC تبدیل می‌کند — منبع‌دار (`auto:search/note/alarm/command/mission`)، با اطمینان محافظه‌کارانهٔ ۰٫۴ و قابل تأیید/رد روی همان صفحه:

- **جست‌وجوها** — هر جست‌وجوی وب/دستیار/گیت‌هاب داخل سایویس (کلیدواژه‌ها بدون ایست‌واژه‌ها)
- **یادداشت‌ها** — جعبهٔ «یادداشت امروز» بالای پروندهٔ شناختی (`uic_quick_note`)؛ دسته‌بندی خودکار به ترجیح/عادت/هدف
- **آلارم‌ها** — فرمان آلارم جدید («آلارم ساعت ۶:۳۰ بذار»، «۲۰ دقیقه دیگر یادآوری کن»، «remind me in 15 minutes») که **آلارم واقعی** در اپ ساعت دستگاه می‌سازد (Intent استاندارد AlarmClock + مجوز SET_ALARM) و در پرونده ثبت می‌شود
- مرز شفاف: تاریخچهٔ جست‌وجوی اپ Google خودِ گوشی با API عمومی قابل خواندن نیست؛ معادلش = جست‌وجوهای داخل سایویس + یادداشتی که خودتان می‌نویسید.

## ✍️ ۶) فونت دستیار + حالت فکر کردن

**وزیرمتن** (مجوز SIL OFL، سه وزن Regular/Medium/Bold) به مخزن اضافه و روی حبابه‌های چت، ورودی و خط «در حال فکر کردن» اعمال شد. حبابهٔ تفکر جدید (`chat_thinking`): «کد ۰۱ · در حال فکر کردن…» با سه نقطهٔ تپندهٔ طلایی و پس‌زمینهٔ شیشه‌ای.

## 🧩 ۷) ابزار گیت‌هاب در اسکریپت‌ها — شناسایی، انتخاب، ادغام

بخش «ادغام‌گر گیت‌هاب» (`git_search_input/scan_button/integrate_button`):

1. **اسکن** بی‌کلید از Search API (با UA درست، همان الگوی تیونر v4)
2. **شناسایی** موارد خاص با امتیازدهی تست‌شده (کاتلین +2، تطبیق عبارت شما +2، ستاره +1/+1، مجوز آزاد +1، حوزهٔ ترید/بات/مارکت +2 → «⭐ خاص» از ۶ به بالا)
3. **انتخاب** با چک‌باکس
4. **ادغام** — فایل‌های بالادستِ مخزن + سرِ README واقعاً گرفته و در «صف ادغام سایویس» (JSON ماندگار + قابل حذف) staged می‌شود و **داسیهٔ ادغام** متن قابل اشتراک می‌سازد. مرز شفاف: کامپایلِ داخل باینری در تسک بعدی توسط من انجام می‌شود — این صف همان لایهٔ صادقانهٔ «پیدا شده → داخل اپ» است.

## 📈 ۹) ترمینال ترید بدون لاگین — چارت زندهٔ هر نماد

بالای صفحهٔ گیت‌وی (قبل از فرم ورود): **چارت لایو تریدینگ‌ویو** (`tv_chart`) با WebView واقعی — هر نمادی که تریدینگ‌ویو بشناسد: `OANDA:XAUUSD`، `NASDAQ:AAPL`، `BINANCE:BTCUSDT`، `FX_IDC:USDIRR`… با فیلد نماد + میان‌برهای طلایی. دیتا از وب‌سوکت خود ویجت جاری است؛ **به اتصال بروکر هیچ نیازی نیست.**

## 🧪 تست‌ها و CI

+۱۴ تست واحد جدید (`SayvisLinkUnitTest`): گیت آفلاین (استثنای درست قبل از I/O)، ریاضیات Mbps، کدها و سقف تاریخچه، پلنر مأموریت، دسته‌بندی پروندهٔ شناختی، امتیاز گیت‌هاب، پارس آلارم فارسی/عربی/انگلیسی، مهاجرت پیش‌فرض حافظهٔ AI — **مجموع ۲۹ تست سبز** + دوقلوی پایتونیِ پارس آلارم/امتیاز/کلیدواژه قبل از push.

۷ دور واقعی CI تا سبزی — هر کدام ریشه‌یابی‌شده، نه حدسی:

| # | خطا | فیکس |
|---|---|---|
| ۱ | فایل مجوز OFL داخل `res/font` → aapt رد کرد | انتقال به `docs/licenses` |
| ۲ | `StatusEvent.code` enum است نه رشته | زنجیرهٔ `.code.code` |
| ۳ | `MissionTask` بدون import در VM | FQN |
| ۴ | `delay` بدون import | import |
| ۵ | رنگ‌های Silver در MissionsScreen | import |
| ۶ | background/shape/Button/Surface در دو صفحه | import |
| ۷ | `R` زیر namespace `com.example` است + تست باکت روزانه | `com.example.R` + بردار ۱ساله |

## 🧾 معیارها

- CI سبز ✅ — run [35575353380](https://github.com/sayo11482/Sayvis1/actions/runs/35575353380)
- Release + APK + SHA256 + ZIP ویندوز ✅ — v5.3.0-build.152
- ۱۰ خواسته: حافظهٔ API ✅ | اتصال پایدار ✅ | دکمهٔ آنلاین/آفلاین + سرعت ✅ | ایجنت راهکار ✅ | پروندهٔ شناختی ✅ | فونت + فکر کردن ✅ | ادغام‌گر گیت‌هاب ✅ | کدهای وضعیت ✅ | چارت بدون لاگین ✅ | زنده بودن ✅

## 🔭 مرز شفاف

قطع اینترنت در محدودهٔ اپ است (OS-wide فقط با VPN و رضایت سیستم). تاریخچهٔ جست‌وجوی اپ Google قابل خواندن نیست؛ معادل درون‌اپی ثبت می‌شود. ادغام نهایی کدهای گیت‌هاب در بیلد بعدی اعمال می‌شود (صف + داسیه الان واقعی و ذخیره‌شده است). امضای APK همچنان debug است.
