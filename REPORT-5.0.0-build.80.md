# گزارش تک ۱۶ — SAYVIS 5.0.0: دستیار فعال، انتخاب ایجنت، مولتی‌تایم‌فریم، ورزش سلیقه‌ای و ایجنت مدیر

## ✅ تحویل‌شده

**Release:** [v5.0.0-build.80](https://github.com/sayo11482/Sayvis1/releases/tag/v5.0.0-build.80)
**APK:** [SAYVIS-5.0.0-debug.apk](https://github.com/sayo11482/Sayvis1/releases/download/v5.0.0-build.80/SAYVIS-5.0.0-debug.apk) + [SHA256](https://github.com/sayo11482/Sayvis1/releases/download/v5.0.0-build.80/SAYVIS-5.0.0-debug.apk.sha256)
**CI:** completed / success — versionCode 16 / versionName 5.0.0

## 🧠 ۱) دستیار بعد از اتصال جمنای واقعاً فعال می‌شود

**ریشه:** چتِ دستیار از `settings.ai.provider` استفاده می‌کرد؛ اگر کلید جمنای وصل بود ولی کلید API ثبت نشده/سوییچ نخورده بود، پاسخ بی‌سروصدا از «هستهٔ محلی» می‌آمد — کاربر می‌دید «دستیار فعال نمی‌شود».

**فیکس:** مسیر چت حالا در هر پیام مغز را resolve می‌کند:
- **AUTO** (پیش‌فرض): بهترین مغز ابریِ تنظیم‌شده انتخاب می‌شود — جمنای اول است؛ یعنی با اتصال جمنای، چت بلافاصله با جمنای جواب می‌دهد حتی اگر سوییچ provider نخورده باشد
- خطای provider هم دیگر بی‌صدا نیست: failover + پیام علت واقعی نمایش داده می‌شود

## 🔀 ۲) انتخاب‌گر «کدام ایجنت پاسخ دهد» در دستیار

نوار chips جدید زیر هدر چت: **خودکار / Gemini / Groq / ChatGPT / Grok / OpenRouter / محلی** — انتخاب شما ذخیره می‌شود (testTag `chat_brain_*`)، هدر چت «مغز فعال» را نشان می‌دهد و اگر کلید چیزی خالی باشد شفاف می‌گوید و خودکار برمی‌گردد.

## 👤 ۳) شناسایی اکانت‌های گوگل دستگاه + میکروفون

- در «مرکز اتصال»: بخش «**اکانت‌های گوگل این دستگاه**» — حساب‌هایی که با گوگل روی گوشی وارد شده‌اند فهرست و با یک لمس «به‌کارگیری» به‌عنوان هویت سایویس لینک می‌شوند (دسترسی GET_ACCOUNTS + audit)
- میکروفون دستیار مسیر واقعی خود را دارد (دکمهٔ میک در چت → تشخیص گفتار سیستم → پیام صوتی به همان خط لولهٔ دستیار) و پس از لینک اکانت، پاسخ‌های صوتی زیر همان هویت اجرا می‌شود

## 📈 ۴) چارت مولتی‌تایم‌فریم + شکار نقطهٔ ورود (MarketsScreen)

- **دادهٔ واقعی ۴ تایم‌فریم** از CoinGecko: M15 (از دیتای ۵دقیقه‌ای)، H1، H4 (ساعتی نمونه‌برداری‌شده)، D1 (روزانه) برای طلا و D1 برای یورو/دلار
- موتور LIT روی هر تایم‌فریم اجرا می‌شود؛ **قواعد همگرایی**: حداقل ۲ تایم‌فریم هم‌نظر + مخالفت‌نکردن H4/D1 → گرید A/B/C
- کارت «نقشهٔ مولتی‌تایم‌فریم و نقطهٔ ورود»: پیل‌های رنگی هر TF (LONG/SHORT/WAIT + RSI/ATR)، تصمیم ترکیبی، و **برنامهٔ ورود کامل**: ورود/حد ضرر/TP1-TP3 با RR — کف سخت **RR≥1:3** داخل موتور شکستنی است
- تست واحد برای هر ۴ قاعدهٔ همگرایی

## 🏋️ ۵) پیشنهاد ورزشی بر اساس سلیقهٔ شما (پیشنهادهای هوشمند)

- **۱۰۰ جستجوی اخیر** شما در سایویس ثبت و موتور سلیقه (`SearchTasteEngine`) علایق را وزن‌دهی می‌کند: کالیستنیکس، تنیس، باشگاه، دویدن، فوتبال، تغذیه (لغت‌نامهٔ دوزبانه)
- خروجی: برنامهٔ واقعی هفتگی — مثلا «کالیستنیکس ۳روزه» (بارفیکس/شنا/پلانک با ست-تکرار)، «تمرین تنیس دیوار و سایه»، تغذیهٔ تمرین — هر پیشنهاد با **دلیل شخصی‌سازی** («چون بارفیکس در جستجوهای شما پررنگ بود»)
- **شفافیت مهم:** تاریخچهٔ سرچ گوگل توسط هیچ اپی مستقیم خوانده نمی‌شود؛ فیلد «چسباندن فعالیت اخیر گوگل» (myactivity.google.com) همان متن را واقعاً ingest می‌کند و سلیقه را دقیق‌تر می‌کند

## 📒 ۶) ایجنت مدیر — دفترچهٔ بیزینس از پیامک‌ها + اینستاگرام

- **اسکن همهٔ پیامک‌ها روی دستگاه** (READ_SMS، همه‌چیز محلی): طبقه‌بند بیزینس دوزبانه فرستندگان را در ۶ دسته می‌گذارد: **تأمین‌کننده مواد اولیه / دفتر پخش / اداره / تفریح / بیزینس عمومی / سایر** با امتیاز و شواهد (کلمات سیگنال)
- هر ورودی شامل: **نام و فامیل** (از مخاطبات)، **شرکت** (استخراج از «شرکت/موسسه/کارخانه»)، **شماره**، **سایت** (URL از متن)، **آدرس** (هیوریستیک «آدرس:/نشانی:») و فرستندهٔ حروفی (Sender Code = نشانهٔ بیزینس)
- UI در ایجنت: دکمهٔ اسکن + خلاصهٔ تعداد هر دسته + کارت‌های مرتب بر اساس امتیاز بیزینسی + audit
- **اینستاگرام:** هندل + بیو + فالوور/فالوئینگ → تشخیص بیزینس/دسته/شواهد → همان دفترچه (provenance=instagram). API اینستاگرام توکن بیزینسی می‌خواهد؛ مسیر روی‌دستگاهی صادقانه است و در UI توضیح داده شده

## 🧪 تست و مسیر CI

- ۱۱ تست واحد جدید (MTF×۵، طبقه‌بند×۵، سلیقه×۴) + دوقلوی پایتونی قبل از هر push
- **۶ دور CI:** تداخل تاریخچه (ریباس روی ریموت — همهٔ فایل‌ها نهایی من شدند) → AccountPicker باقیمانده نه، بلکه: constants بیرون‌از‌scope در codec، تایپ ضمنی Compose، importهای جامانندهٔ ۳ صفحه، فیلد `Target.rr`، و باگ کلاس کاراکتری `[^\\]` در regex سیگنال‌ها (باز هم الگوی `\\` دوگانه — مرجع v3.1.0 دوباره نجات‌دهنده بود) → ✅

## 🧾 معیارها

- CI سبز ✅ — run [35027574095](https://github.com/sayo11482/Sayvis1/actions/runs/35027574095)
- Release + APK ✅ — v5.0.0-build.80
- دستیار فعال + انتخاب ایجنت ✅ | اکانت‌های گوگل دستگاه ✅ | مولتی‌تایم‌فریم با نقطهٔ ورود ✅ | ورزش سلیقه‌ای ✅ | ایجنت مدیر (SMS+اینستاگرام) ✅
- گزارش فارسی ✅ — همین سند
